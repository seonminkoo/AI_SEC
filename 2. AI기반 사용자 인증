# AI 기반 사용자 인증

### 무자각 인증 데이터 수집

## 4. 데이터 가공

### 수집 RAW 데이터
데이터 필드의 의미를 보고 처리 방법 결정해야 함  
딥러닝모델에는 숫자만 들어가므로 명목형 데이터도 숫자로 변환해서 넣어야 함  

### 특징 추출
#### Raw data에서 일부 추출해서 씀  
ex. 눈 동자의 크기로 align, 얼굴 영역만 두고 배경 검게 칠함   

#### 파형 
- 주파수 분할  
- MFCC

#### Raw data를 가공해서 만들어냄  
데이터를 보고 통계정보로 만들어서 따로 특징값으로 사용함  

### 코딩
### 범주형 데이터는 코딩해줘야 함  
- 라벨 인코딩 : 값에 따라 번호를 붙히는 것  

- FC1 ( Label Encoding ) : 필드마다 고유한 모든 값에 0 부터 번호 부여  
    -> 값의 크고 작고가 의미가 없음  
- FC2 (One Hot Encoding ) : 값의 개수만큼 차원을 만들어 값에 따라 해당 위치만 1 로 세팅  
- FC3 ( 변형 One Hot Encoding ) : 사용자 별로 모델이 존재하므로 , 사용자가 사용한 값의 개수만큼 차원을 만듦   

### 결측치 처리  

### Normalize
 - 서명의 정규화  
    - 서명을 시퀀스 데이터로 표현해서 샘플링 되는 좌표를 길이로 표현!  
    - 시작점, 크기, 길이를 동일하게   
    
- 트랙잭션 data
  - 스트링 : 공백 제거   
  - steming  

## 5. 모델링 
### 학습 데이터 보강  





#### 실습
1.
2. 코 위치 큰원으로 그리기
3. 입술이 들어가는 사각형 그리기
4. 얼굴에 흰색 마스크 그려넣기 : 선  두께를 -1로, 흰색은 (255,255,255)
5. 추출된 얼굴 영역을 별도의 파일로 저장하기: Image [y: y+h, x: x+w]  
 두 명 이상 포함된 사진에서 얼굴 추출하여 box 그리기

```
